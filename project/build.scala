import sbt._
import Keys._

/**
 * @brief Build properties for SBT.
 * 
 */
class build extends Build {
	/* TODO : set the properties for :
	 * 1/ Compilation.
	 * 2/ Run (directly plugged-in to the compiler).
	 * 3/ Tests : run the test from the test/ folder. */
}